import 'package:flutter/material.dart';

class AppColors {
  // prevent instantiation
  AppColors._();

  // Backgrounds
  static const Color backgroundColor = Color(0xFFFFFFFF);

  // Primary
  static const Color primaryColor = Color(0xFF1A1A1A);
  static const Color primaryLightColor = Color(0xFF808080);

  // Secondary
  static const Color secondaryColor = Color(0xFF999999);
  static const Color secondaryButtonColor = Color(0xFFCCCCCC);

  // Accent (for price tags, badges, sale labels)
  static const Color accentColor = Color(0xFFED1010);

  // Social buttons
  static const Color facebookButtonColor = Color(0xFF1877F2);
  static const Color googleButtonColor = Color(0xFFFFFFFF);

  // UI Elements
  static const Color dividerColor = Color(0xFFE6E6E6);
  static const Color borderColor = Color(0xFFCCCCCC);
  static const Color cardColor = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimaryColor = Color(0xFF1A1A1A);
  static const Color textSecondaryColor = Color(0xFF808080);
  static const Color textHintColor = Color(0xFF999999);

  // Status
  static const Color successColor = Color(0xFF0C9409);
  static const Color errorColor = Color(0xFFED1010);
  static const Color warningColor = Color(0xFFFFC107);

  // Star rating
  static const Color starColor = Color(0xFFFFA928);
}